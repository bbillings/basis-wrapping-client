{
  "scripts": {
    "test": "jest",
    "unwrap": "node -e 'require(\"./index\").unwrapAndLog(\"'$npm_config_token_id'\")'",
    "wrap": "node -e 'require(\"./index\").wrapAndLog(\"'$npm_config_data'\")'"
  },
  "dependencies": {
    "axios": "^0.27.2"
  },
  "devDependencies": {
    "chance": "^1.1.8",
    "jest": "^28.1.2",
    "jest-when": "^3.5.1"
  }
}